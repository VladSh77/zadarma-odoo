# ТЗ — zadarma_odoo (Zadarma VoIP Integration for Odoo 17)

> Повний чеклист реалізованих та запланованих функцій.
> ✅ — готово | 🔲 — заплановано | ❌ — скасовано

---

## Фаза 1 — Базова інтеграція (Production) ✅

### Моделі та архітектура

| Функція | Статус |
|---------|--------|
| `zadarma.call` — модель запису дзвінка | ✅ |
| `zadarma.import` — TransientModel для ретроімпорту | ✅ |
| Розширення `res.company` — API credentials | ✅ |
| Розширення `res.users` — SIP ID поле | ✅ |
| Розширення `res.partner` — Smart Button + список дзвінків | ✅ |
| Розширення `crm.lead` — лічильник дзвінків | ✅ |

### Webhook обробка

| Подія | Статус | Примітка |
|-------|--------|----------|
| `NOTIFY_END` — вхідний PBX дзвінок | ✅ | caller_id → phone |
| `NOTIFY_OUT_END` (calltype=callback_leg2) — вихідний | ✅ | leg2 only |
| `NOTIFY_RECORD` — запис розмови готовий | ✅ | MP3 → filestore |
| GET echo верифікація Zadarma | ✅ | `?zd_echo=...` |
| Дедублікація за `call_id` | ✅ | |

### Click-to-Call

| Функція | Статус |
|---------|--------|
| `action_zadarma_call()` на `res.partner` | ✅ |
| Zadarma Callback API: `GET /v1/request/callback/` | ✅ |
| Номер клієнта з `+` prefix (CallerID routing) | ✅ |
| Параметр `sip=` для CallerID-by-destination | ✅ |
| CallerID routing `+380*` → UA / `+48*` → PL (в Zadarma кабінеті) | ✅ |

### Записи розмов

| Функція | Статус |
|---------|--------|
| Завантаження MP3 через тимчасовий URL | ✅ |
| Збереження в `ir.attachment` (постійне) | ✅ |
| `recording_url` → `/web/content/{id}?download=true` | ✅ |
| Fallback: тимчасовий URL якщо завантаження зламалось | ✅ |

### Ретроспективний імпорт

| Функція | Статус |
|---------|--------|
| Imпорт за довільний діапазон дат | ✅ |
| Пагінація `skip/limit=1000`, затримка 1с | ✅ |
| HTTP 429 retry (3 спроби, затримки 3/6/9 сек) | ✅ |
| Дедублікація: існуючі `call_id` пропускаються | ✅ |
| Нотатки позначаються `(імпорт)` | ✅ |

### Бізнес-логіка

| Функція | Статус |
|---------|--------|
| Визначення напрямку за довжиною caller_id (≤5 цифр = вихідний) | ✅ |
| Пошук партнера (SQL suffix, останні 9 цифр) | ✅ |
| Пріоритет партнерів з нечисловими іменами | ✅ |
| Авто-створення ліда для невідомих номерів | ✅ |
| Chatter нотатка від імені менеджера (за SIP ID) | ✅ |
| HMAC-SHA1 підпис запитів | ✅ |

---

## Фаза 2 — Розширення (Roadmap)

### IVR та привітання

| Функція | Статус |
|---------|--------|
| Голосове привітання для +48 (польська мова) | 🔲 |
| Голосове привітання для +380 (українська мова) | 🔲 |
| GDPR/RODO попередження про запис (для +48) | 🔲 |
| IVR черга — налаштування в Zadarma | 🔲 |

### Картка партнера

| Функція | Статус |
|---------|--------|
| Smart Button "Дзвінки" у верхній частині картки | ✅ (лічильник є) |
| Окрема вкладка дзвінків (не лише в Chatter) | 🔲 |
| Список каналів клієнта (окрема вкладка) | 🔲 |

### Безпека та цілісність даних

| Функція | Статус |
|---------|--------|
| HTML cleanup в Chatter (рендеринг тегів) | ✅ |
| Read-only для `zadarma.call` (блокування Create/Delete/Edit) | 🔲 |
| Call Loop Prevention (через Zadarma кабінет) | ✅ (задокументовано) |

### Multi-user

| Функція | Статус |
|---------|--------|
| SIP `100` (менеджер 1) | ✅ |
| SIP `101`, `102`, `103` | ✅ |
| Тестування multi-user статистики | 🔲 |

### Технічний борг

| Функція | Статус |
|---------|--------|
| `zadarma_callerid_rules` — очистити або реалізувати | 🔲 |
| Timezone handling (Zadarma → UTC) | 🔲 |
| S3/зовнішнє сховище для записів (при потребі) | 🔲 |
